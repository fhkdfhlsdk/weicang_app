<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".hand_control"
            android:label="@string/handle_activity" />
        <activity android:name=".Motor1"
            android:label="@string/up_and_down"/>
        <activity android:name=".WaitingActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@drawable/textview_border" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controlActivity"
            android:label="@string/title_activity_control" />
    </application>

</manifest>